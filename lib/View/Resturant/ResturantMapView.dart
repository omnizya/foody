import 'package:flutter/material.dart';

class ResturantMapView extends StatelessWidget {
  const ResturantMapView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('TODO'),
    );
  }
}
