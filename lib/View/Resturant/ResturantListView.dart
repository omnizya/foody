import 'package:flutter/material.dart';

class ResturantListView extends StatelessWidget {
  const ResturantListView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('TODO'),
    );
  }
}
